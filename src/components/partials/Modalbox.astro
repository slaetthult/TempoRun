---
const {id = '', classes = ''} = Astro.props;
const className = `modalbox ${id} ${classes}`;
---

<partial-modalbox class={className} data-js="modalbox" style="display: none;">

    <div class="modalbox__wrapper">
        <div class="modalbox__header">
            <slot name="modalbox-header"/>
        </div>
        <slot/>
        <div class="modalbox__footer">
            <slot name="modalbox-footer"/>
        </div>
    </div>

</partial-modalbox>


<script>
    class PartialModalbox extends HTMLElement {
        constructor() {
            super();
        }
        connectedCallback() {
            import('@scripts/partials/modalbox')
                .then((module) => {
                    const { modalbox } = module;
                    modalbox.init(this);
                })
                .catch((error) => {
                    console.error('Error loading module:', error);
                });

        }

    }
    customElements.define('partial-modalbox', PartialModalbox);
</script>